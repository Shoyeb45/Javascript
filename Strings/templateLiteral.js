const product = {
    name : "Pen",
    price : 10,
    ratingStar : 4
};

let description = `The name of product is ${product.name}, price is INR ${product.price} and rating is ${product.ratingStar} star.`;

console.log(description);